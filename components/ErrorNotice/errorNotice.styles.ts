import styled from 'styled-components'

export const ErrorNoticeWrapper = styled.h2`
  margin-top: 64px;
  text-align: center;
  font-weight: 300;
  line-height: 64px;

  .sad {
    font-size: 72px;
  }
`
